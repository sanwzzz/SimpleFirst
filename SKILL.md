---
name: simple-first
description: 在实现、修复、重构或评审代码时，优先选择简单、清晰、易维护的方案，避免过度设计、无效抽象和不必要依赖，同时保留必要的校验、安全、注释与测试。适用于多语言软件开发任务；不用于纯文档写作或与开发无关的请求。
---

# SimpleFirst

以最低认知成本完成需求。简单意味着容易理解和维护，不等于代码行数最少。

## 决策顺序

在修改代码前理解需求并阅读相关调用链，然后依次判断：

1. 仓库中是否已有可复用的实现、类型或既有模式。
2. 目标语言的标准库、当前框架或平台原生能力是否已经覆盖需求。
3. 已安装依赖是否能够清晰地解决问题。
4. 以上均不满足时，再编写最小且完整的自定义实现。

不要为推测性的未来需求增加抽象、配置项、扩展点、通用框架或第三方依赖。用户未明确要求复杂方案时，先实现当前需求所需的简单方案；若简单方案存在重要限制，在结果中说明限制及升级条件。

## 设计与实现

- 遵守仓库现有架构、模块边界和代码模式。不能为了减少文件数量而破坏职责划分。
- 不创建只有一个实现且没有实际替换需求的接口、工厂、策略或包装层。
- 不重复实现仓库、标准库、框架、平台或现有依赖已经提供的能力。
- 在多个方案都正确时，选择改动范围更小、边界行为正确、认知成本更低的方案。
- 优先使用清晰命名和直接控制流。不要为了写成一行而降低可读性。
- 只修改完成任务所需的文件和行为，不进行无关重构或格式化。
- 用户明确要求完整方案或既有架构要求抽象时，按要求实现，不重复争论简化方案。

## Bug 修复

把问题描述视为症状而不是根因。修改共享方法前查找它的调用方和相邻路径，优先在能够覆盖所有相关调用方的根因位置修复。避免只给单一路径增加补丁，也不要借修复之名扩大改动范围。

## 正确性边界

不得为了简化代码而删除或弱化：

- 信任边界的参数校验与权限检查。
- 安全控制和敏感信息保护。
- 防止数据丢失、状态损坏或错误吞没的异常处理。
- 并发、事务、一致性和资源释放所需的保障。
- 用户明确要求的行为及仓库规定的质量要求。

如果采用存在明确上限的简单方案，例如 O(n²) 扫描、全局锁、固定容量或朴素启发式，应在代码附近用简短中文注释说明适用边界和升级条件。

## 注释与文档

- 所有新建或修改的模块、类、构造器、函数、方法和测试方法，必须使用目标语言惯用的文档注释格式编写简洁中文说明，描述职责或行为。
- Java 使用 Javadoc，Python 使用 docstring，JavaScript/TypeScript 使用 JSDoc/TSDoc；其他语言遵循仓库和语言的原生文档约定。
- 每个参数都必须说明含义、格式、单位、所有权或关键约束。在支持结构化标签的格式中使用 `@param`、`Args` 等惯用写法；不支持时在文档注释中自然说明。
- 有返回值时必须说明返回结果；在支持结构化标签的格式中使用 `@return`、`Returns` 等惯用写法。
- 复杂逻辑、边界校验以及少见的框架或第三方 API，必须在使用位置添加中文行内注释，解释原因和约束。
- 若仓库明确规定注释语言或格式，以仓库规范为准。
- 不添加只复述代码、没有额外信息的注释。不要用注释掩盖本可通过命名和结构解决的可读性问题。

## 验证

测试范围与变更风险匹配。简单修改执行最小相关测试；新增接口、非平凡分支、共享能力以及安全或数据路径，应覆盖主要流程和关键边界。优先使用仓库现有测试框架和测试模式，不为单个测试引入新框架。

完成后简要说明实际改动、验证结果，以及确实存在的限制；不展开未实施的推测性设计。
